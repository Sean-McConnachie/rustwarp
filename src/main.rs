use rustwarp::device::core::*;
use rustwarp::{typenum, wvec2, wvec3, wvec4, wvec_call};

struct Test {
    t1: wvec2!(f32, 16),
}

fn main() {
    // cpu_transform::run_test_transform();
    // pollster::block_on(device::setup::State::new());
    // let x = size_of!(device::core::Pix);
    // dbg!(x);
}
